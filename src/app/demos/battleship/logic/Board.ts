export class Board {
  private board;
  constructor() {
    this.board = this.createBoard();
  }

  private createBoard() {
    const newBoard = [];

    for (const y of Array(10)) {return}
  }
}
